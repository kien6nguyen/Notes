# Production environment — copy to .env.prod and fill in values

# Database
DB_DATABASE=laravel
DB_USERNAME=sail
DB_PASSWORD=change_me_strong_password
DB_ROOT_PASSWORD=change_me_root_password

# Frontend API URL (set to your server's public IP or domain)
VITE_API_URL=http://YOUR_SERVER_IP:8000/api
